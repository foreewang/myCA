# 工控机服务端口与启动说明

本文适用于部署支线 `deploy/improve-20260827`，目标目录固定为 `D:\colony_system`。

## 1. 服务与端口

| 项目 | 配置 |
| --- | --- |
| 服务 | Colony System FastAPI |
| 监听协议 | TCP |
| 监听地址 | `0.0.0.0` |
| 监听端口 | `8000` |
| 本机存活检查 | `http://127.0.0.1:8000/health` |
| 局域网存活检查 | `http://<工控机IP>:8000/health` |
| OpenAPI 页面 | `http://<工控机IP>:8000/docs` |

项目只有这一个网络监听服务。视觉、自动对焦、相机和 Modbus 均由 API 服务统一管理，不需要再启动独立端口；其中 MVS 原生调用运行在 API worker 管理的可终止本机子进程中。相机 IP 和本机相机网卡 IP 不是本项目的服务监听端口；电机使用 `COM3` 等串口，也不是 TCP 端口。

硬件只能由一个进程、一个 worker 控制。启动时必须使用 `--workers 1`，禁止使用 `--reload`，也不要同时启动第二个 API 实例。

当前 API 没有认证、授权或 TLS。`0.0.0.0` 会监听工控机所有网卡，只能部署在隔离、受控的生产网段，禁止映射到公网。

## 2. 部署内容

将部署支线的发布包解压到：

```text
D:\colony_system
```

不要直接复制开发电脑当前工作目录。开发工作目录含 Git 历史、缓存和大量被忽略的运行图像；发布包应由部署支线的提交生成。

部署支线不包含历史任务结果、测试输出、训练依赖或训练数据；保留完整 `tests/test_*.py`、必要的离线数据校验工具与 `tools/release_gate.py` 作为上线软件门禁。程序会按需创建 `data/task_index`、任务输出目录、`logs/api_server.log` 和 `data/api_server.lock`。

### 4x 模型必须单独复制

Git 不包含生产 ONNX 权重。默认 4x 检测必须另行复制已验收的模型发布目录，例如：

```text
D:\colony_system\vision\models\ipsc_4x\production\
├─ model_manifest.json
├─ detector.onnx
└─ segmenter.onnx
```

实际文件名以 `model_manifest.json` 为准。接口任务中的 `detect.model_dir` 应指向该目录。缺少正式 manifest 或任一权重时，采集仍可用，但默认 4x 检测任务会失败。

## 3. 首次安装环境

使用 64 位 Python 3.10。代码语法最低要求是 3.10，GPU 依赖与海康 MVS Python 包也应按该版本安装。在 PowerShell 中执行：

```powershell
Set-Location D:\colony_system
py -3.10 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
# 工控机已验收 NVIDIA/CUDA 时，只安装 GPU 锁定环境
python -m pip install -r requirements-lock-py310-gpu.txt

# 无 GPU 的 CPU 部署改用这一条，不要与 GPU 环境混装
# python -m pip install -r requirements-lock-py310-cpu.txt

# 上线软件门禁的叠加依赖；不参与 API 运行
python -m pip install -r requirements-test-lock-py310.txt
```

`requirements.txt` 与两个 `requirements-vision-runtime-*.txt` 只用于开发解析和维护，不作为生产重装入口。生产只允许安装一份对应的 `requirements-lock-py310-*.txt`；锁文件是发布候选基线，仍须通过本机 GPU/CPU、MVS 和完整业务验收后才能归档为正式 wheelhouse。使用 CPU 运行时时，任务还必须显式设置 `detect.provider: "cpu"`。

还需安装海康 MVS SDK。工控机的 SDK 导入目录统一为 `D:/colony_system/MvImport`，主配置、第三方自动对焦配置及相机控制器兜底值均已按此设置。部署前必须把真实的 `MvImport` 文件复制到该目录，并确认其中存在 `MvCameraControl_class.py`、`CameraParams_header.py` 和 `CameraParams_const.py`；项目部署包不包含也不会自动生成 MVS SDK 文件。

## 4. 启动前检查

先核对现场参数：

- `config/camera.yaml`：相机序列号、相机 IP、MVS SDK 路径、分辨率。
- `config/autofocus.yaml`：相机网卡 IP、COM 口、调焦范围、输出路径。
- `config/objectives.yaml`：物镜与调焦位置、碰撞限位、COM 口。
- `config/plates.yaml`：A1、脉冲/mm、方向和软件限位。
- `config/handoff.yaml`：机械臂交接点、COM 口和从站号。
- 生产模型目录：正式 manifest、两份 ONNX 及其哈希一致。

执行静态检查：

```powershell
Set-Location D:\colony_system
.\.venv\Scripts\python.exe tools\release_gate.py
.\.venv\Scripts\python.exe -m workflow.config_validator
```

软件门禁会先强制 64 位 CPython 3.10、CPU/GPU 二选一、运行与测试锁文件中的全部精确版本、关键 DLL 导入和 `pip check`，再编译源码并运行完整 pytest 回归；核心任务/运动/视觉测试文件缺失、收集数量低于 200、任何失败或 skip 都是发布阻断。配置校验会检查本机 MVS SDK 路径，因此 SDK 未安装或路径不正确时也应失败。

GPU 模型发布包可用下列命令预加载并校验 manifest、文件哈希、ONNX 接口和 CUDA provider：

```powershell
.\.venv\Scripts\python.exe -c "from vision.vision.model_runtime import load_model_bundle; b=load_model_bundle(r'D:\colony_system\vision\models\ipsc_4x\production', prefer_cuda=True); print(b.runtime_backend)"
```

CPU 部署将 `prefer_cuda=True` 改成 `prefer_cuda=False`。`/health` 和 YAML 配置校验都不会检查模型文件，不能替代这一步。

全新部署包故意不携带旧电脑的 `data/objective_state.json`。首个包含 `objective_name` 的任务会发生真实物镜与调焦轴运动，并在成功后生成状态文件。执行首个任务前必须确认急停、零点、物镜位置、碰撞限位和运动区域。升级已有工控机时，应保留现场已有的 `data/objective_state.json`，不要用源码包清空运行状态。

## 5. 启动服务

推荐双击或在命令行运行仓库自带脚本：

```bat
cd /d D:\colony_system
start_api.bat
```

等价的手动命令：

```powershell
Set-Location D:\colony_system
$env:COLONY_API_WORKERS = "1"
$env:UVICORN_WORKERS = "1"
$env:WEB_CONCURRENCY = "1"
$env:COLONY_LOG_REDACT_SENSITIVE = "1"
.\.venv\Scripts\python.exe -m workflow.deployment_preflight
.\.venv\Scripts\python.exe -m workflow.config_validator
.\.venv\Scripts\python.exe -m uvicorn workflow.api_server:app --host 0.0.0.0 --port 8000 --workers 1 --lifespan on --timeout-graceful-shutdown 45
```

出现以下关键信息表示监听已建立：

```text
Application startup complete.
Uvicorn running on http://0.0.0.0:8000
```

该窗口必须保持运行。停止服务前先按下一节完成安全停机。

## 6. 安全停机

不要直接 `taskkill /F`。服务关闭会有界停止任务队列，并尝试停止、提交活动录像及释放进程锁；它不会替代硬件急停，也不会自动停止仍在运行的往复运动。

1. 停止上位机继续提交任务。
2. 有活动任务时，调用 `POST /api/tasks/{task_id}/cancel`，轮询任务状态直至终态。
3. 往复运动中时，调用 `POST /api/stage/reciprocation/stop`，确认状态为 `stopped` 或 `failed`。
4. 录像中时，调用 `POST /api/camera/record/stop`。若返回 `CAMERA_RECORD_STOP_BLOCKED`，先完成第 2 步；随后轮询录像状态，确认 `state=idle`、`error=null` 且 `last_video.frame_count>0`。
5. 确认 `/api/hardware/status` 返回 `busy=false`。
6. 在服务窗口只按一次 `Ctrl+C`，等待进程正常退出。任务清理与相机录像提交最坏约需 45 秒，现场服务管理器应至少给 90 秒总停止宽限；不要因暂时显示 `Waiting for ... shutdown` 再按一次 Ctrl+C 强制取消 lifespan。

出现人员或设备危险时优先使用物理急停；软件取消和停止接口不是急停。

## 7. 存活检查与防火墙

本机检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

预期返回：

```json
{"status":"ok"}
```

`/health` 只证明 HTTP 进程能够响应，不检查相机、串口、电机或模型是否可用。还可查询 `/api/hardware/status`、`/api/camera/record/status` 和 `/api/stage/reciprocation/status`；这些接口同样不能替代真实硬件验收。

查看端口监听进程：

```powershell
Get-NetTCPConnection -LocalPort 8000 -State Listen |
    Select-Object LocalAddress, LocalPort, OwningProcess
```

需要局域网访问时，以管理员 PowerShell 放行可信调用方网段。先把示例网段替换成现场真实的受控网段：

```powershell
$trustedSubnet = "192.168.1.0/24"
New-NetFirewallRule -DisplayName "Colony System API 8000" `
    -Direction Inbound -Action Allow -Protocol TCP -LocalPort 8000 `
    -Profile Private -RemoteAddress $trustedSubnet
```

然后在可信网段内的另一台电脑访问 `http://<工控机IP>:8000/health`。

## 8. 开机自启动

Windows 任务计划程序建议使用：

| 设置 | 值 |
| --- | --- |
| 程序 | `C:\Windows\System32\cmd.exe` |
| 参数 | `/c "D:\colony_system\start_api.bat"` |
| 起始于 | `D:\colony_system` |
| 实例规则 | 已运行时不启动新实例 |

设置完成后重启工控机，并再次检查 `/health`、`logs/api_server.log` 和端口监听进程。

## 9. 运行数据与磁盘

5120×5120 Mono8 BMP 单张约 25 MiB，单孔扫描可能产生数百 MiB。任务图片和结果不会自动轮转，需监控 D 盘剩余容量并制定备份、保留期和清理流程。

只在任务、录像和往复运动全部停止后清理已归档的旧任务目录。不要删除当前任务目录、`data/api_server.lock`、`data/objective_state.json` 或写入中的 `.part` 文件。API/监督器日志位于 `logs/api_server.log`，MVS 相机子进程日志位于 `logs/camera_worker.log`，均由程序轮转。

## 10. 最小验收顺序

1. `/health` 本机返回 `ok`。
2. 局域网调用方可以访问 TCP 8000。
3. `/api/hardware/status` 为 `busy=false`、`/api/camera/record/status` 为 `recording=false`、`/api/stage/reciprocation/status` 为 `stopped`。
4. MVS 能识别指定序列号相机；COM 口和四个从站与现场一致。
5. 人工确认急停、零点、碰撞限位和物镜状态。
6. 低速完成单轴、物镜、调焦和单孔采集验收。
7. 确认模型发布目录后，再验收单孔 4x 检测。
8. 最后验收补偿、handoff 和整板任务。
9. 用 `python tools\camera_record_soak.py --cycles 20 --record-seconds 10 --serial-number <现场序列号> --report-path logs\camera_record_soak_release.json` 完成至少 20 轮录像；脚本会用 OpenCV 独立打开容器、解码首/中/末帧、核对 API 与 AVI 的帧数/FPS/时长/尺寸、检查文件非空及输出目录无残留 `.part.avi`，任一不符即失败。
10. 录像期间执行快照、兼容曝光的扫描和自动对焦；不兼容的曝光/增益必须明确拒绝，不能静默沿用录像参数。
11. 录像中断开相机网线或执行经批准的 SDK 卡死注入，确认状态接口仍能响应、`worker_pid` 被替换、`worker_restart_count` 增加，恢复链路后无需重启 API 即可再次录像。
12. 在 `STARTING`、`RECORDING`、`STOPPING` 各状态各做一次正常服务停止，确认正式文件只提交一次、进程在停止宽限内退出、日志无 `ASGI lifespan appears unsupported`、`CancelledError` 或 `Overlapped ... pending operation at deallocation`。

以上门禁结果、API/相机日志、输出 AVI、`camera_record_soak` JSON 报告、依赖预检 JSON 和现场网络参数须随发布版本归档。任一项未执行或失败，都不能判定上线完成。
