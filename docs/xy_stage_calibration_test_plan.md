# 电机 1/2（XY 位移台）标定与分支功能测试方案

## 1. 本次标定基线

| 项目 | 标定值 |
| --- | ---: |
| 电机1 / X 从站 | 1 |
| 电机2 / Y 从站 | 2 |
| X pulses/mm | 65536 |
| Y pulses/mm | 131072 |
| X 物理硬限位 | -1295041 ～ 6525977 |
| Y 物理硬限位 | -1100000 ～ 9284715 |
| 共用 safety_margin | 131072 pulse |
| X 可执行安全范围 | -1163969 ～ 6394905 |
| Y 可执行安全范围 | -968928 ～ 9153643 |
| 交接点 | (5000000, 0) |

`safety_margin=131072` 等于 Y 轴 1 mm、X 轴 2 mm。物理硬限位保持实测值，不把安全余量混入 `x_min/x_max/y_min/y_max`。

四种板型的 A1 起始观测点（4x，画面右侧刚贴内径内壁）：

| 板型 | A1 |
| --- | ---: |
| 6-well | (6198780, 7287429) |
| 12-well | (5774368, 8116360) |
| 24-well | (6110380, 8508572) |
| 48-well | (5848352, 8942329) |

换孔使用 `well_step` 示教脉冲步长，不再使用 `well_diameter_mm + well_gap_mm`。内径只用于孔内扫描圆。示教点写在 `well_teach`：行末端、列末端用于均分步长，对角点只记录。

孔内扫描方向保持 `x_stage_sign_for_view_right=-1`、`y_stage_sign_for_view_down=-1`。换孔不再使用 `row_stage_sign` / `col_stage_sign`。

## 2. 部署包静态检查

部署支线不包含开发测试套件。在 `/opt/colony_system` 使用项目虚拟环境执行静态编译和配置校验：

```bash
cd /opt/colony_system
.venv/bin/python -m compileall -q workflow devices vision third_party
.venv/bin/python -m workflow.config_validator --plates config/plates.yaml --handoff config/handoff.yaml --camera config/camera.yaml --autofocus config/autofocus.yaml --objectives config/objectives.yaml
```

完整自动化回归应在 `improve` 开发支线执行；这里的检查只确认部署文件可导入且现场 YAML 通过校验，不能替代硬件验收。

## 3. 上电前检查

1. 清空运动区域，确认硬件急停可用，先用厂家软件低速点动。
2. 读取电机1/2当前位置，确认驱动器零点没有再次被重置；本项目没有自动 Homing。
3. 分别接近四个物理限位，但不要用程序反复撞限位；确认位置读数与本表一致后退回至少 2 mm。
4. 先装空载测试板，不使用有价值样品；物镜保持在不会碰撞的安全高度。

## 4. 方向和导程验收

以交接点 `(5000000, 0)` 为 A 点。首次执行时降低速度并让操作员手持急停。

X 轴 10 mm 往复：

```bash
.venv/bin/python tools/stage_reciprocation_accuracy.py --axis x --point-a-x 5000000 --point-a-y 0 --point-b-x 5655360 --point-b-y 0 --cycles 10 --warmup-cycles 1 --profile-vel 100000 --plate-type 24-well --execute
```

Y 轴 10 mm 往复：

```bash
.venv/bin/python tools/stage_reciprocation_accuracy.py --axis y --point-a-x 5000000 --point-a-y 0 --point-b-x 5000000 --point-b-y 1310720 --cycles 10 --warmup-cycles 1 --profile-vel 100000 --plate-type 24-well --execute
```

验收要求：

- X 正脉冲：培养板物理方向 A1→A2，画面特征向左。
- Y 正脉冲：培养板物理方向 A1→B1，画面特征向上。
- 实测 10 mm 行程与量具结果一致；误差阈值由现场精度要求确定，不能只用驱动器位置反馈代替量具结果。
- A→B→A 连续 10 次无撞限位、卡死、异常声响或丢步；CSV/JSON 结果保存在 `data/stage_accuracy`。

## 5. 板坐标验收

逐板低速验证 A1、行末端、列末端，再看对角。示教点如下：

| 板型 | A1 | 行末端 | 列末端 | 对角（只记录） |
| --- | ---: | ---: | ---: | ---: |
| 6-well | (6198780,7287429) | A3=(1064474,7280843) | B1=(6198780,2227451) | B3=(1064507,2227451) |
| 12-well | (5774368,8116360) | A4=(654283,8116360) | C1=(5774368,1372091) | C4=(654283,1372091) |
| 24-well | (6110380,8508572) | A6=(-222148,8508572) | D1=(6100380,970117) | D6=(-226076,970117) |
| 48-well | (5848352,8942329) | A8=(-161565,8942352) | F1=(5854353,468155) | F8=(-166565,499227) |

软件按 `well_step` 网格计算的中间孔/最远孔为：

| 板型 | A2 | B1（网格） | 最远孔（网格） |
| --- | ---: | ---: | ---: |
| 6-well | (3631627,7284136) | (6198780,2227451) | B3=(1064474,2220865) |
| 12-well | (4067673,8116360) | (5774368,4744226) | C4=(654283,1372092) |
| 24-well | (4843874,8508572) | (6107047,5995754) | D6=(-232149,970118) |
| 48-well | (4989792,8942332) | (5849552,7247494) | F8=(-155568,468175) |

示教判据与 A1 相同：画面右侧刚贴该孔内径内壁。最远孔是 A1 + 行列步长的网格点，与对角示教点允许有亚毫米残差（48 孔 F8 示教约为 (−166565, 499227)）。若整组点出现相同偏差，重新标定该板 A1；若偏差随行/列累积，重新示教行末端/列末端并改 `well_step`，不能只修改单个孔坐标，也不能再用直径加间隙去凑孔距。

## 6. 扫描覆盖与安全验收

使用当前物镜 FOV、`overlap=0.1` 对 A1 做离线规划。扫描圆半径为 `内径/2 − 半视野`，只覆盖培养面内径。A 行向上扫描会超出 Y 安全上限 `9153643`；规划会丢掉越界点并继续。现场行程不允许把整板往 Y 负向挪，因此接受 A 行上沿裁剪。不要减小 `safety_margin` 去换上沿：12/24 孔 4x 最上行离硬限不足 0.3 mm，48 孔 4x 最上行已超过硬限。

| 板型 | 4X | 10X |
| --- | --- | --- |
| 6-well | 通过（上沿裁剪） | 通过（上沿裁剪） |
| 12-well | 通过（上沿裁剪） | 通过（上沿裁剪） |
| 24-well | 通过（上沿裁剪） | 通过（上沿裁剪） |
| 48-well | 通过（上沿裁剪） | 通过（上沿裁剪） |

现场不能把整板往 Y 负向挪。起始点本身仍必须落在安全范围内，否则任务在规划阶段失败。

## 7. handoff 与回归验收

1. 空载执行 `load_in`、`unload_out`，两者均应到 `(5000000,0)`。
2. 将测试配置中的交接点临时改到安全范围外，任务必须在发运动指令前失败；测试后恢复配置。
3. 验证到位误差超过 `arrival_tolerance_pulse=3000` 时不会返回机器人 ready 状态。
4. 依次执行：单孔 4X 采集、跨孔 A1→A2→B1、视觉补偿、自动调焦、4X/10X 物镜切换、handoff。
5. 最后执行目标业务板型的整板任务；任何 `stage_limit_precheck` 失败都应先调整机械安装或重新标定，不绕过保护。
